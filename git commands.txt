git --int to intialize the new repository in working directory
git --commit -m "message" to commit the message
git remote add origin 'raghava second branch'
git add -A to add to all files in staging area
